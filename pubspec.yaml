name: panorama_viewer_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  panorama_viewer: ^1.0.2
  flutter_riverpod: ^2.4.9
  imageview360: ^1.3.0
  zoom_widget: ^2.0.1
  file_picker: ^6.1.1
  desktop_drop: ^0.4.4
  dotted_border: ^2.1.0
  dchs_motion_sensors: ^1.0.2
  flutter_cube: ^0.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1

flutter:

  uses-material-design: true
